# 🚀 TONIGHT'S QUICK START GUIDE

This workflow now lives in the "Tonight workflow (live odds)" section of [QUICK_START.md](QUICK_START.md).

TL;DR:
- Set `ODDS_API_KEY` (or `THE_ODDS_API_KEY`).
- From repo root: `python generate_tonight_picks.py --live` 2-3 hours before first tip.
- Review outputs in `testing/results/predictions/` (paper mode only).
