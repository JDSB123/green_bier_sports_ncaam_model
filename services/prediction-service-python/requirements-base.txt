# Base / runtime requirements for Codespaces quick start
# Keep only packages needed to run the service; heavy numeric/ML deps are moved to requirements-heavy.txt

# Web framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0

# Database (sync driver)
sqlalchemy==2.0.25
psycopg2-binary==2.9.9

# HTTP + utilities
httpx==0.26.0
requests==2.31.0
redis==5.0.1
structlog==24.1.0
msal==1.26.0

# Testing helpers (kept light for quick setup)
pytest==7.4.4
pytest-asyncio==0.23.3
